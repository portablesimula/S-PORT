#include <d:\mc\include\math.h>

main()
{
        double x, y, z;

        y = exp(x);
        y = log(x);
        z = pow(x, y);
        y = sqrt(x);
        y = log10(x);
        y = sin(x);
        y = cos(x);
        y = tan(x);
        y = asin(x);
        y = acos(x);
        y = atan(x);
        z = atan2(x, y);
        y = sinh(x);
        y = cosh(x);
        y = tanh(x);
}
